{
  "name": "@hst/contracts",
  "version": "0.1.0",
  "description": "Shared enums, permission keys, error codes and DTO types for HST (ADR-001)",
  "license": "UNLICENSED",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
